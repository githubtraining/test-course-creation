title: test-course-creation
tagline: A brand new course on Learning Lab
description: Trying to replicate an error --- nothing to see here!
template:
  repo: test-course-creation-template
  name: test-course-creation
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
